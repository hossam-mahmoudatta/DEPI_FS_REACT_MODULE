// I want to create a function that will take the first parameter as the operation
// and the rest of the parameters will be stored into "rest"
// so that can i make the operations on them

function hosaOperation (operation, ...numbers) {
    if (operation == '+') {
      
    }
};
